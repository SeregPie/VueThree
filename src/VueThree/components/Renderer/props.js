export default {
	alpha: {
		type: Boolean,
		default: false,
	},

	antialias: {
		type: Boolean,
		default: true,
	},

	clearAlpha: {
		type: Number,
		default: 1,
	},

	clearColor: {
		type: [Number, String],
		default: 0x000000,
	},

	preserveDrawingBuffer: {
		type: Boolean,
		default: false,
	},
};
