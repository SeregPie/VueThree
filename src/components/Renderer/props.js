export default {
	antialias: {
		type: Boolean,
		default: true,
	},

	alpha: {
		type: Boolean,
		default: false,
	},

	clearColor: {
		type: [Number, String],
		default: 0x000000,
	},

	clearAlpha: {
		type: Number,
		default: 1,
	},

	preserveDrawingBuffer: {
		type: Boolean,
		default: false,
	},
};
